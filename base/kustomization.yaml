---
# Kustomize конфигурация для базового стека мониторинга
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: monitoring

resources:
  - namespace.yaml

# Общие labels для всех ресурсов
commonLabels:
  app.kubernetes.io/part-of: monitoring-stack
  monitoring: enabled
